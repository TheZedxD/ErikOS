# Network
HOST=127.0.0.1
PORT=8000

# Security / Terminal
TERMINAL_TIMEOUT_SECONDS=10
# Comma-separated whitelist of safe commands (customize as needed)
TERMINAL_WHITELIST=echo,ls,dir,uname,date,whoami,ping

# Profiles/data (created by install)
USERS_DIR=DRIVE/users
LOGS_DIR=logs
